name: CI

on:
  push:
  pull_request:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder workflow step
        run: echo "CI placeholder for future automation pipeline"